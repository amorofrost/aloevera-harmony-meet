# Environment variables for AloeVera Harmony Meet

# API Mode: "mock" or "api"
# - "mock": Use local mock data (default for development)
# - "api": Call real backend REST APIs
VITE_API_MODE=mock

# Backend API URL (only used when VITE_API_MODE=api)
VITE_API_BASE_URL=http://localhost:5000
